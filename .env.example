# ======================================
# Vaultify Environment Variables
# ======================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env to git - it's already in .gitignore
# ======================================

# Database Configuration
# For production: Use full JDBC URL with your remote database
# For local dev: Use localhost
DB_URL=jdbc:postgresql://localhost:5432/vaultify
DB_USER=your_database_user
DB_PASSWORD=your_secure_password_here

# Remote Ledger Server Configuration
# For production: Use your deployed ledger service URL
# For local dev: Use http://localhost:3000/api
LEDGER_API_URL=http://localhost:3000/api

# Docker Compose Configuration (optional)
# Only needed if using Docker Compose for local development
POSTGRES_DB=vaultify_db
POSTGRES_USER=vaultify_user
POSTGRES_PASSWORD=secret123

